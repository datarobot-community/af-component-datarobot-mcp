---
name: Reusable Deploy E2E
on:
  workflow_call:
    inputs:
      use-case-id:
        description: "Key from fixtures/e2e/use-cases.yaml"
        required: true
        type: string
      skip-destroy:
        description: "Skip pulumi destroy after deploy (debug only)"
        required: false
        default: "false"
        type: string
      deploy-timeout-minutes:
        description: "Job timeout for deploy-e2e (Docker builds may need longer)"
        required: false
        default: "5"
        type: string
    secrets:
      DATAROBOT_API_TOKEN:
        required: true
      PULUMI_ACCESS_TOKEN:
        required: false
      PULUMI_CONFIG_PASSPHRASE:
        required: false
jobs:
  deploy-e2e:
    name: "Deploy E2E (${{ inputs.use-case-id }})"
    runs-on: "ubuntu-latest"
    timeout-minutes: ${{ fromJSON(inputs.deploy-timeout-minutes) }}
    container:
      image: ghcr.io/astral-sh/uv:python3.11-bookworm
    defaults:
      run:
        shell: bash
    env:
      UV_CACHE_DIR: /.uv_cache
      DATAROBOT_API_TOKEN: ${{ secrets.DATAROBOT_API_TOKEN }}
      PULUMI_ACCESS_TOKEN: ${{ secrets.PULUMI_ACCESS_TOKEN }}
      DATAROBOT_ENDPOINT: ${{ vars.DATAROBOT_ENDPOINT != '' && vars.DATAROBOT_ENDPOINT || 'https://app.datarobot.com/api/v2' }}
      USE_CASE_ID: ${{ inputs.use-case-id }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup caching
        uses: ./.github/actions/setup-caching
        with:
          python-version: python3.11
          cache-key-suffix: e2e-deploy-${{ inputs.use-case-id }}
          dependency-files: |
            template/{{mcp_app_name_file}}/pyproject.toml.jinja
            fixtures/e2e/infra/pyproject.toml

      - name: Load use case
        id: load
        run: |
          set -euo pipefail
          uvx --with pyyaml python "${GITHUB_WORKSPACE}/fixtures/e2e/load_use_case.py" "${USE_CASE_ID}" >> "${GITHUB_ENV}"
          echo "STACK_NAME=ci-e2e-${USE_CASE_ID}-${GITHUB_RUN_ID}" >> "${GITHUB_ENV}"
          echo "SESSION_SECRET_KEY=ci-e2e-${USE_CASE_ID}-${GITHUB_RUN_ID}" >> "${GITHUB_ENV}"

      - name: Install Pulumi CLI
        run: |
          curl -fsSL https://get.pulumi.com | sh
          echo "/github/home/.pulumi/bin" >> "${GITHUB_PATH}"

      - name: Render template
        uses: ./.github/actions/render-copier-instance
        with:
          copier-args: "--defaults --trust --vcs-ref=HEAD"

      - name: Install Task
        uses: arduino/setup-task@v2

      - name: Run deployment E2E
        id: deploy
        run: bash "${GITHUB_WORKSPACE}/fixtures/e2e/run_deploy_e2e.sh"
        env:
          RENDERED_DIR: ./rendered
          DATAROBOT_API_TOKEN: ${{ secrets.DATAROBOT_API_TOKEN }}
          DATAROBOT_ENDPOINT: ${{ vars.DATAROBOT_ENDPOINT != '' && vars.DATAROBOT_ENDPOINT || 'https://app.datarobot.com/api/v2' }}
          PULUMI_CONFIG_PASSPHRASE: ${{ secrets.PULUMI_CONFIG_PASSPHRASE != '' && secrets.PULUMI_CONFIG_PASSPHRASE || 'ci-e2e-pulumi-passphrase' }}

      - name: Stage Pulumi state for cleanup
        if: always()
        run: |
          set -euo pipefail
          mkdir -p cleanup-artifacts/pulumi-home cleanup-artifacts/rendered
          if [[ -d /github/home/.pulumi ]]; then
            cp -a /github/home/.pulumi/. cleanup-artifacts/pulumi-home/
          fi
          if [[ -f rendered/.env ]]; then
            cp rendered/.env cleanup-artifacts/rendered/.env
          fi
          if [[ -d rendered/infra ]]; then
            cp -a rendered/infra cleanup-artifacts/rendered/
          fi

      - name: Upload Pulumi state for cleanup
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-pulumi-${{ inputs.use-case-id }}-${{ github.run_id }}
          if-no-files-found: ignore
          retention-days: 1
          path: cleanup-artifacts/

      - name: Mark deploy failure
        if: failure() && steps.deploy.outcome == 'failure'
        run: echo "::error::Deployment E2E failed (case=${CASE_NAME}, stack=${STACK_NAME})"

  destroy-e2e:
    name: "Destroy E2E (${{ inputs.use-case-id }})"
    needs: deploy-e2e
    if: always() && inputs.skip-destroy != 'true'
    runs-on: "ubuntu-latest"
    timeout-minutes: 15
    container:
      image: ghcr.io/astral-sh/uv:python3.11-bookworm
    defaults:
      run:
        shell: bash
    env:
      UV_CACHE_DIR: /.uv_cache
      DATAROBOT_API_TOKEN: ${{ secrets.DATAROBOT_API_TOKEN }}
      PULUMI_ACCESS_TOKEN: ${{ secrets.PULUMI_ACCESS_TOKEN }}
      DATAROBOT_ENDPOINT: ${{ vars.DATAROBOT_ENDPOINT != '' && vars.DATAROBOT_ENDPOINT || 'https://app.datarobot.com/api/v2' }}
      USE_CASE_ID: ${{ inputs.use-case-id }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup caching
        uses: ./.github/actions/setup-caching
        with:
          python-version: python3.11
          cache-key-suffix: e2e-destroy-${{ inputs.use-case-id }}
          dependency-files: |
            template/{{mcp_app_name_file}}/pyproject.toml.jinja
            fixtures/e2e/infra/pyproject.toml

      - name: Load use case
        run: |
          set -euo pipefail
          uvx --with pyyaml python "${GITHUB_WORKSPACE}/fixtures/e2e/load_use_case.py" "${USE_CASE_ID}" >> "${GITHUB_ENV}"
          echo "STACK_NAME=ci-e2e-${USE_CASE_ID}-${GITHUB_RUN_ID}" >> "${GITHUB_ENV}"

      - name: Install Pulumi CLI
        run: |
          curl -fsSL https://get.pulumi.com | sh
          echo "/github/home/.pulumi/bin" >> "${GITHUB_PATH}"

      - name: Render template
        uses: ./.github/actions/render-copier-instance
        with:
          copier-args: "--defaults --trust --vcs-ref=HEAD"

      - name: Download Pulumi state
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          name: e2e-pulumi-${{ inputs.use-case-id }}-${{ github.run_id }}
          path: e2e-artifacts

      - name: Restore deploy artifacts
        run: |
          set -euo pipefail
          if [[ -f e2e-artifacts/rendered/.env ]]; then
            mkdir -p rendered
            cp e2e-artifacts/rendered/.env rendered/.env
          fi
          if [[ -d e2e-artifacts/rendered/infra ]]; then
            mkdir -p rendered
            cp -a e2e-artifacts/rendered/infra rendered/
          fi

      - name: Run destroy E2E
        run: bash "${GITHUB_WORKSPACE}/fixtures/e2e/destroy_deploy_e2e.sh"
        env:
          RENDERED_DIR: ./rendered
          PULUMI_STATE_DIR: ${{ github.workspace }}/e2e-artifacts/pulumi-home
          PULUMI_CONFIG_PASSPHRASE: ${{ secrets.PULUMI_CONFIG_PASSPHRASE != '' && secrets.PULUMI_CONFIG_PASSPHRASE || 'ci-e2e-pulumi-passphrase' }}
